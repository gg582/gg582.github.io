---
layout: knowledge-base
title: 유타 티팟 후속 - 급격한 기하 꺾임만 찾아 국소 FXAA 적용하기
subtitle: 주둥이 구멍·뚜껑 림·가림 경계만 분석하고, 매끈한 실루엣은 건드리지 않는 CPU 후처리
date: 2026-07-24 18:00:00 +0900
categories:
- mathematics
taxonomy:
  category: mathematics
  subcategory: computer-graphics
  order: 2
difficulty: advanced
keywords:
- Utah Teapot
- Software Renderer
- Local FXAA
- Anti-Aliasing
- G-buffer
- Depth Buffer
- Normal Buffer
- Curvature
---

이 글은 [패치 용접·국소 곡률 보정·점광원까지 만든 이전 글](/mathematics/유타-티팟-X11-소프트웨어-렌더링-패치-용접과-국소-보정/)의 후속이다. 이번에는 “화면의 모든 에지에 FXAA를 걸지 말고, 렌더링 분석 중 **급격하게 꺾이는 기하 지점**만 찾아 그 주변에 적용하자”는 기준으로 AA 경로를 다시 만들었다.

실행 가능한 핵심 소스(`main.cpp`, `CMakeLists.txt`, `teapot.bpt`, 구현 노트)는 [utah-teapot-geometry-guided-aa.zip](/assets/files/utah-teapot-geometry-guided-aa.zip)으로 첨부했다.

## 1. 바뀐 판단: 실루엣 전체가 아니라 기하 이벤트

이전 실험의 문제는 깊이·법선 불연속을 넓게 스텐실로 만들고 FXAA를 적용했다는 점이다. 그러면 매끈하게 휘어진 몸통의 외곽도 후보가 된다. 이는 “화면에서 에지가 보인다”는 사실만으로 보간을 결정한 것이며, 급격한 형상 변화만 처리하려는 목적과 맞지 않는다.

새 구현은 한 픽셀 `p`와 4방향 이웃 `q`에서 다음 값을 수집한다.

```text
depthJump  = max_q |z_p - z_q| / min(z_p, z_q)
normalBend = max_q (1 - dot(N_p, N_q))
backgroundCount = background 이웃의 수
curvature = 래스터화 때 보간해 둔 기하 곡률 proxy
```

후보가 되는 것은 다음 셋 중 하나다.

```text
apertureOrCorner = backgroundCount >= 2
                OR (backgroundCount >= 1 AND curvature > 0.07 AND normalBend > 0.04)

abruptBend      = curvature > 0.08 AND normalBend > 0.07
occlusionKink   = depthJump > 0.035 AND normalBend > 0.04
```

`backgroundCount == 1`인 평범한 실루엣은 그 자체로 후보가 아니다. 곡률과 법선 변화가 함께 커야 한다. 반대로 주둥이의 구멍처럼 둘 이상의 방향이 배경과 닿는 개구부, 뚜껑 림처럼 배경 접촉과 강한 방향 변화가 함께 있는 부분, 서로 다른 깊이의 표면이 만나는 가림 경계는 후보가 된다.

따라서 이 방법은 정확한 미분기하학적 주곡률 계산도, 진짜 MSAA도 아니다. 하지만 CPU 래스터라이저가 이미 계산한 depth·normal·curvature를 이용해 “어디에만 비용을 써야 하는가”를 보수적으로 결정하는 실용적 규칙이다.

## 2. 실제 파이프라인

```text
single-sample triangle rasterisation
  -> colour + depth + normal + curvature G-buffer
  -> aperture / abrupt-bend / occlusion-kink analysis
  -> sharp-feature stencil only
  -> local directional FXAA
  -> PPM / X11
```

AA 후보로 선택된 픽셀에서만 최종 색의 휘도를 계산한다.

```text
L = 0.299 R + 0.587 G + 0.114 B
```

상하와 좌우의 휘도 차를 비교해 에지 방향을 고르고, 그 방향을 가로지르는 두 이웃을 평균내어 중심 색에 제한적으로 섞는다. `--aa=fxaa`는 고정된 보수적 혼합량을 쓰고, `--aa=adaptive`는 위 분석의 강도(score)와 휘도 대비가 클수록 혼합량을 조금 올린다.

```text
featureScore = max(curvature / 0.16,
                   normalBend / 0.20,
                   depthJump / 0.08)

fixed FXAA amount    = 0.16 + 0.10 * contrast
adaptive FXAA amount = 0.14 + 0.18 * featureScore * contrast
```

둘 다 한 번의 **국소 FXAA**다. `adaptive`가 FXAA 뒤에 화면 전체를 다시 블러링하는 두 번째 필터는 아니다. 메시 위치·깊이·법선 자체는 수정하지 않는다.

## 3. 비교를 위한 카메라 구도

AA의 차이를 보려면 카메라가 중요하다. 정면의 낮은 구도에서는 뚜껑과 몸체의 경계가 겹치거나 짧아져서, 보간 전후를 읽기 어렵다. 아래 비교는 위에서 약간 내려다보는 3/4 구도(`theta=1.5`, `phi=0.55`, `radius=5.4`)를 사용했다. 이 시점에서는 뚜껑 림과 몸체 사이의 어두운 틈·밝은 림이 화면을 길게 가로지른다.

패치당 분할 수는 비교를 드러내려고 `8`로 낮췄다. 이 값은 최종 품질 권장값이 아니라, 단일 샘플 픽셀 계단과 선택적 보간의 차이를 확인하기 위한 시험 조건이다.

| 단일 샘플 (`off`) | 급격한 지점의 국소 FXAA | 강도 적응형 국소 FXAA |
| --- | --- | --- |
| ![AA off](/assets/images/utah-teapot-aa/aa_off.png) | ![Local FXAA](/assets/images/utah-teapot-aa/aa_fxaa.png) | ![Adaptive local FXAA](/assets/images/utah-teapot-aa/aa_adaptive.png) |

아래는 뚜껑 림을 세 배 확대한 결과다. 왼쪽부터 `off`, `fxaa`, `adaptive`다. 몸통 전체의 하이라이트는 그대로 두고, 림의 급격한 기하 변화로 판정된 픽셀 근처에서만 전이 픽셀이 추가된다.

![뚜껑 림의 국소 FXAA 전후 확대](/assets/images/utah-teapot-aa/aa_lid_rim_comparison.png)

## 4. 실행과 비교

```bash
cmake -S . -B build-aa -DCMAKE_BUILD_TYPE=Release
cmake --build build-aa -j

# X11 창에서 보기
./build-aa/teapot --unify=yes --aa=adaptive

# 본문과 같은 비교용 정지 이미지
./build-aa/teapot --still off.ppm      --unify=yes --tess 8 --size 512x384 \
  --camera 1.5 0.55 5.4 --aa=off
./build-aa/teapot --still fxaa.ppm     --unify=yes --tess 8 --size 512x384 \
  --camera 1.5 0.55 5.4 --aa=fxaa
./build-aa/teapot --still adaptive.ppm --unify=yes --tess 8 --size 512x384 \
  --camera 1.5 0.55 5.4 --aa=adaptive
```

옵션의 의미는 다음과 같다.

- `--aa=off`: 후처리 없이 단일 샘플 래스터화만 사용한다.
- `--aa=fxaa`: 분석에서 통과한 급격한 기하 후보에만 고정 강도의 국소 FXAA를 적용한다.
- `--aa=adaptive`: 같은 후보에만 적용하되, 곡률·법선 변화·깊이 변화가 클수록 FXAA 혼합량을 적응적으로 조절한다. 기본값이다.

## 5. MSAA와의 경계

이 구현은 실제 MSAA가 아니다. MSAA는 래스터화 때 픽셀 내부의 여러 샘플 위치에서 coverage와 깊이를 판정한다. 여기서는 이미 확정된 단일 샘플 색을 국소적으로 보간할 뿐이므로, 가는 선·알파 컷아웃·서브픽셀 기하에 대한 coverage 정보는 되찾지 못한다.

다만 이번 sharp-feature stencil은 다음 단계의 선택적 MSAA에도 그대로 쓸 수 있다. 후보가 있는 8×8 타일만 2×2 sub-pixel coverage로 재래스터화하고 나머지는 단일 샘플로 유지하면, 그때는 실제 선택적 MSAA가 된다. 현재 단계는 그 비용을 들이기 전에 후보 위치와 필터 범위를 검증하는 구현이다.
